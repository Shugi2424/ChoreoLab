export const enumTypeDefs = `#graphql
  enum Apparatus {
    hoop
    ball
    clubs
    ribbon
  }

  enum BodyCategory {
    jump
    balance
    pivot
  }

  enum AgeCategory {
    senior
    junior
  }

  enum RCriteriaType {
    throw
    catch
    general
  }

  enum ArtistryType {
    character
    dance
    dynamicChange
    effect
  }
`;
